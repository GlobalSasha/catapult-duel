import { GAME_CONFIG } from "./core/gameConfig";

export const GAME_WIDTH = GAME_CONFIG.viewport.width;
export const GAME_HEIGHT = GAME_CONFIG.viewport.height;
export const RENDER_WIDTH = 2560;
export const RENDER_HEIGHT = 1440;
export const RENDER_SCALE = RENDER_WIDTH / GAME_WIDTH;
